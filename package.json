{
  "name": "money-map",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/finance.js && node --check src/app.js"
  }
}
